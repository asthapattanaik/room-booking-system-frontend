specVersion: "v1beta"
serviceId: "hotel-room-reservation"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "hotel-room-reservation-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
